<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only. Cleartext is permitted solely for the emulator's host loopback and
     localhost, so a self-hosted Valhalla (`http://10.0.2.2:8002/route`) or Photon
     (`http://10.0.2.2:2322/api`) endpoint from local.properties works; everything else stays
     https-only like the platform default. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
